tmp_dir: "/tmp"
expe_dir: "/home/user"
test_expe: False
tplgy_name:
  - "clique-fav"  # Full connectivity
rn_type:
  - "no_rn"       # Standard scenario (without RN)
nodes_count:
  - 3
id_run_boundaries:
  min: 0
  max: 1          # Do one run, with the first uptime schedule (uptimes_schedules/0-60.json)
